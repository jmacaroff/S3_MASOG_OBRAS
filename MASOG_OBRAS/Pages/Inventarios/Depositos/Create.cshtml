@page
@model MASOG_OBRAS.Pages.Inventarios.Depositos.CreateModel

@{
    ViewData["Title"] = "Create";
}

<h1>Nuevo depósito</h1>
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div class="form-group">
                <label asp-for="Deposito.Id" class="control-label"></label>
                <input asp-for="Deposito.Id" class="form-control" />
                <span asp-validation-for="Deposito.Id" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Deposito.Descripcion" class="control-label"></label>
                <input asp-for="Deposito.Descripcion" class="form-control" />
                <span asp-validation-for="Deposito.Descripcion" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Deposito.Direccion" class="control-label"></label>
                <input asp-for="Deposito.Direccion" class="form-control" />
                <span asp-validation-for="Deposito.Direccion" class="text-danger"></span>
            </div>

            @* Mensaje que muestra el error en la validación. *@

            @if (Model.MessageError != null)
            {
                <div class="alert alert-warning" role="alert">@Model.MessageError</div>
            }

            <div class="form-group">
                <input type="submit" value="Guardar" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="Index">Volver</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
