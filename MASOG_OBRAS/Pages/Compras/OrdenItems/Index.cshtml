@page
@model MASOG_OBRAS.Pages.Compras.OrdenItems.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.OrdenItem[0].Cantidad)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.OrdenItem[0].Precio)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.OrdenItem[0].Observacion)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.OrdenItem[0].Orden)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.OrdenItem[0].Producto)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.OrdenItem) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Cantidad)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Precio)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Observacion)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Orden.Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Producto.Id)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.Id">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
